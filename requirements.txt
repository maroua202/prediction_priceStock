streamlit==1.28.1
numpy==1.24.3
pandas==2.0.3
yfinance==0.2.32
scikit-learn==1.3.1
matplotlib==3.8.0
# Note: TensorFlow is not included because Streamlit Cloud often cannot install TF wheels.
# If you want full LSTM training in production, deploy with Docker/Render where TF can be installed.
